# Optional dependencies for side_quests/retrieval_benchmark
# Keep these separate to avoid breaking environments with strict CUDA/driver constraints.

pyterrier-dr>=0.4.0
faiss-gpu-cu11>=1.13.0

# This side quest is configured for GPU-only FAISS.
# With your driver (535.309.01), CUDA 12.1-compatible wheels are expected to be supported.
